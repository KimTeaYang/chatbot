# ===========================================
# 환경 변수 템플릿 파일
# 이 파일을 .env로 복사하고 실제 값으로 변경하세요
# ===========================================

# 기본 애플리케이션 설정
PROJECT_NAME=Gemini Chatbot API
VERSION=1.0.0
DESCRIPTION=AI 챗봇 API 서비스

# 서버 설정
HOST=0.0.0.0
PORT=8000
DEBUG=true
LOG_LEVEL=INFO

# API 설정
API_V1_STR=/api/v1

# CORS 설정
ALLOWED_HOSTS=["*"]

# AI 서비스 설정 (필수 - 실제 키로 변경 필요)
GOOGLE_API_KEY=your_google_api_key_here
AI_MODEL=gemini-2.0-flash-exp
AI_TEMPERATURE=0.7
AI_MAX_TOKENS=1000

# 데이터베이스 설정 (선택사항)
# DATABASE_URL=postgresql://username:password@localhost:5432/chatbot_db

# Redis 설정 (선택사항)
# REDIS_URL=redis://localhost:6379/0

# 보안 설정 (필수 - 강력한 키로 변경 필요)
SECRET_KEY=change-this-to-a-secure-secret-key
ACCESS_TOKEN_EXPIRE_MINUTES=30